apiVersion: v1
kind: Service
metadata:
  name: ui-svc
  namespace: retail-store-ns
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
    name: http
  selector:
    app: ui